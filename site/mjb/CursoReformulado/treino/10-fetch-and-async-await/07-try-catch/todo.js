/**
 * Create a try catch block and just console log x as x is not defined it will run the catch part in the catch part
 * simple console log 'Error: ' +  the error per se
 * 
 * Now create a function double(number) and if the number isNaN throw new Error number + ' is not a number'
 * Return number *2
 * 
 * Then use the try catch block to console log the double or the error
 */
