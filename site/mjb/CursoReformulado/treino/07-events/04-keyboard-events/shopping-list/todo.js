/*
Create a const itemInput and with document getElementById the id item-input
*/


/*
Just one press, hit the key
Create a const arrow function that gets the e object and just console log keypres
Then create a addEventListener keypress that runs the function onKeyPress
*/

/*
Release the key
Create another const arrow function onKeyUp that tegs the e object and just console log keyup
Then create a addEventListener keyup that runs the function onKeyUp
*/


/*
Holding the key
Create another const arrow function onKeyDown that tegs the e object and if the e keyCode iqual to 13 alert You pressed enter
If e.code iqual to Digit1 console log You presed 1
if e.repeat console.log You are holding down + e.key
console log 'Shift: ' + e.shiftKey it will console Shift: true or Shift: false 
Do the same above for ctrlKey and altKey
And at the end check if e.shiftKey and e.key === K and console log You hit shift + K
Then create a addEventListener keyup that runs the function onKeyUp
*/

