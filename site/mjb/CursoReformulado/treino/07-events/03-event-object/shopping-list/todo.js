/*
Create a const logo and get the the img tag
*/

/*
Create a function onClick and get the e object event and right below this function create an addEventListener click calling this function
so every time the image is clicket the onClick cuntion is called. Inside the onClick function console log:
e.target
e.currentTarget
e.type
e.timeStamp
e.clientX
e.clientY
e.offsetX
e.offsetY
e.pageX
e.pageY
e.screenX
e.screenY
See the result for eatch one
See that you can use e.target to change properties fo an element like e.target.style.backgroundColor = 'black'
*/


/*
- `target` - The element that triggered the event
- `currentTarget` - The element that the event listener is attached to (These are the same in this case
- `type` - The type of event that was triggered
- `timeStamp` - The time that the event was triggered
- `clientX` - The x position of the mouse click relative to the window
- `clientY` - The y position of the mouse click relative to the window
- `offsetX` - The x position of the mouse click relative to the element
- `offsetY` - The y position of the mouse click relative to the element
- `pageX` - The x position of the mouse click relative to the page
- `pageY` - The y position of the mouse click relative to the page
- `screenX` - The x position of the mouse click relative to the screen
- `screenY` - The y position of the mouse click relative to the screen
*/

/*
Dinamyc values
Create a function onDrag passing the e object event and set the h1 textContent to a string like 'X e.clientx Y e.clientY
then add the addEventListener drag calling the function onDrag
*/


// e.preventDefault() method prevents the default behavior
/*
Select the a tag and add an addEventListener click passing the e object and prevent the default behavior and then just consolo log Link was clicked
 */


