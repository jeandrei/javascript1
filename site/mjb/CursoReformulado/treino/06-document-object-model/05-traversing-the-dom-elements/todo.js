let output;

// Get children elements from a parent

// Create a const parent and with querySelector get the class parent assigned to it

// Create assign to output the parent children, it will return an HTMLCollection you can access each element as in array

// Output = parent children 1 innerText

// output = paren children 1 className

// output = parent children 1 nodeName

// parent children 1 innerText = 'Child Two'

// parent children 1 style color = 'red'

// firstElementChild and lastElementChild are used for the next element like the next li inside ul
// if use firstChild it will get the first node child 
// parent firstElementChild innerText = 'Child One'

// parent lastElementChild.innerText = 'Child Three'

// Get parent elements from a child

// create a const child = class .child use querySelector

// output = child parentElement

// child parentElement style border 1px solid #ccc

// child parentElement style padding 10px

// Get sibling elements 

// create a const secondItem and With pseudo selector nth  select the second element inside child class

// output = secondItem

// output = secondItem nextElementSibling

// secondItem nextElementSibling style color = green use nextElementSibling if 
//you use nextSibling it will get the next node and not the next element

// secondItem nextElementSibling style color = orange

console.log(output);
