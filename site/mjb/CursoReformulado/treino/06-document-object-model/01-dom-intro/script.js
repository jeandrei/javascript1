// Global window object
// Log the window object

// The document object is part of the window object
// Log the document object pay attention, use console .dir to see the attributes console.log will just show the code

// We can access DOM elements directly with properties
// Log the body element

// Log the link zero from document

// We can set properties of elements
// Set the body to <h1>Hello from body</h1>

// We also have methods to select elements more directly
// select the element by id main an then change the html to <h1>Hello from main</h1>

// We can use selectors as css, with querySelector select the id main and h1 and change the text to hello

