// replaceWith() Method
/*
  Create a function replaceFirstItem and in the function create a const firstItem that will get the li first child
  then create a element li  and assign to li the textContent Replace First
  then in the firstItem use replaceWith and replace with the new li
*/


// OuterHTML Property
/*
  Create a function replaceSecondItem() and inside the function create a const secondItem and using nth select the second item of li
  then using outerHTML replace secondItem with the html code <li>Replaced Second</li>
*/

// Replace All Items
/*
  Create a function replaceAllItems 
  inside the function create a const lis and select all li
  and for each li insede lis outerHTML to assign the code <li>Replace All</li>
  as a bonus using ternary operator ? : if the index is 1 set <li>Second Item</li> else <li>Item</li>
*/


// replaceChild() Method
/* Create a function replaceChildHeading
  inside the function as we want to replace the h1 and it is after the header, we first create a 
  const header and select header
  then we create a const h1 and select the header h1
  then we create a const l2 with an element h2
  then set h2 id to app-title
  set h2 textContent to Shopping List
  and in the end header replaceChild h2, h1
*/

