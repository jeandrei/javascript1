//* Seção 07 Aula 81  */
// remove() Method

/*Create a function removeClearButton that remove the clear button the id of the button is clear the function needs to have a const clearBtn and then remove clearBtn*/
//removeClearButton();

// removeChild() Method
/*Create a function removeFirstItem to remove first item o ul, first create a const ul and select ul then create a const li and select the li first child
and in the end ul remove first child li */
// removeFirstItem();

// Other examples
/*Create a function removeItem(itemNumber) that remove a specific item, the item is a parameter 
first create a const ul and select ul then create a const li and select the nth child itemNumber as a hint use backticks
*/
// removeItem(2);


/*Create a function removeItem2(itemNumber), it is the same as above but this time in the li selector use querySelectorAll and remove itemNumber -1 allowing to call 
removeItem2(1) to remove the first element instead of removeItem2(0)  */
//removeItem2(itemNumber)

/*Create another function removeItem3(itemNumber) and in that function create a const li and querySelectorAll li then just remove the item selecting the index as
li[itemNumber] remove*/
//removeItem3(itemNumber)

/*The small one, create a arrow function removeItem4  and select all li, and in the index itemNumber - 1 remove the item*/
//removeItem4(2);
