/* Create a function createButton(classes) that creates a button  
create a function createIcon(classes) that creates a icon
then create a function createNewItem(item) that add an item to the list*/


