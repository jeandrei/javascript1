// Ways to declare a variable
// `var`, `let`, & `const`

// Declare uma variável com o let firstName com o valor John

// Declare uma const lastName com o valor Doe

// Declare uma variável let age com o valor 30

// Log age

// With let, we can declare a value without assigning a value
// Declare uma let score e não atribua nenhum valor

// Atribua o valor 1 a variável score

// Log score

// We can still manipulate arrays and objects using const
// Declare um array arr com os valores de 1 a 4

// Adicione o valor 5 a arr

// Log arr

// Declare um objeto person com o atributo name:'Brad

//Altere o atributo do objeto person.name pare John e adicione o atributo email com o valor brad@gmail.com

// Log person

// Declare multiple values at once declare as variáveis let a, b e c em uma única vez

// Declare as const d = 10, e = 20 e f = 30 em uma única vez

// Log d

// Log a
